Server:
  port: 8181

spring:
  datasource:
    username: root
    password: 111www111
    url: jdbc:mysql://localhost:3306/countries?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC& cachePrepStmts=true&useServerPrepStmts=true&rewriteBatchedStatements=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      order_insets: true
      ddl-auto: update
    show-sql: true

